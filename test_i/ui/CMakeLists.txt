include (${CMAKE_CURRENT_SOURCE_DIR}/../../src/ui/scripts/PreUIFramework.cmake)
#include (${CMAKE_CURRENT_SOURCE_DIR}/../../src/ui/scripts/SetUIFramework.cmake)

if (GTK_SUPPORT)
 message (STATUS "found gtk support")
 if (GTK2_SUPPORT)
  message (STATUS "found gtk2 support")
 endif (GTK2_SUPPORT)
 if (GTK3_SUPPORT)
  message (STATUS "found gtk3 support")
 endif (GTK3_SUPPORT)
# include (${CMAKE_CURRENT_SOURCE_DIR}/../../src/ui/scripts/SetGTKVersion.cmake)
endif (GTK_SUPPORT)

if (WXWIDGETS_SUPPORT)
 message (STATUS "wxWidgets_DEFINITIONS: ${wxWidgets_DEFINITIONS}")
 message (STATUS "wxWidgets_DEFINITIONS_DEBUG: ${wxWidgets_DEFINITIONS_DEBUG}")
 message (STATUS "wxWidgets_CXX_FLAGS: ${wxWidgets_CXX_FLAGS}")
 message (STATUS "wxWidgets_USE_FILE: ${wxWidgets_USE_FILE}")
 if (WIN32)
#  add_definitions(-DUNICODE -D_UNICODE)
  add_definitions (-D__WXMSW__)
  add_definitions (-DWXUSINGDLL)
  if (MSVC)
   add_definitions (-DwxMSVC_VERSION_AUTO)
  endif (MSVC)
  # *NOTE*: re-enable ANSI mode
  add_definitions (-DwxUSE_UNICODE=1)
  add_definitions (-DwxUSE_UNICODE_WCHAR=0)
  add_definitions (-DwxUSE_UNICODE_UTF8=1)
  add_definitions (-DwxUSE_STD_STRING=1)
 endif (WIN32)
 add_definitions (-DwxUSE_GUI=1)
 if (CMAKE_BUILD_TYPE STREQUAL Debug)
  foreach (DEFINITION ${wxWidgets_DEFINITIONS_DEBUG})
   add_definitions (-D${DEFINITION})
  endforeach ()
  add_definitions (-D__WXDEBUG__)
 else ()
  foreach (DEFINITION ${wxWidgets_DEFINITIONS})
   add_definitions (-D${DEFINITION})
 endif (CMAKE_BUILD_TYPE STREQUAL Debug)
endif (WXWIDGETS_SUPPORT)

#########################################

if (GTK_SUPPORT)
 add_subdirectory (gtk)
endif (GTK_SUPPORT)
if (WXWIDGETS_SUPPORT)
 add_subdirectory (wxwidgets)
endif (WXWIDGETS_SUPPORT)

